# eslint-plugin-meiday

meiday custom

## Installation

You'll first need to install [ESLint](https://eslint.org/):

```sh
npm i eslint --save-dev
```

Next, install `eslint-plugin-meiday`:

```sh
npm install eslint-plugin-meiday --save-dev
```

## Usage

Add `meiday` to the plugins section of your `.eslintrc` configuration file. You can omit the `eslint-plugin-` prefix:

```json
{
    "plugins": [
        "meiday"
    ]
}
```


Then configure the rules you want to use under the rules section.

```json
{
    "rules": {
        "meiday/rule-name": 2
    }
}
```

## Rules

<!-- begin auto-generated rules list -->
TODO: Run eslint-doc-generator to generate the rules list.
<!-- end auto-generated rules list -->


